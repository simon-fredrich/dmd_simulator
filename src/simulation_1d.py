import numpy as np
import matplotlib.pyplot as plt
from 1


mirror_size = 10
mirror =





alpha = 10*2*np.pi/360

for x in np.linspace(0, ):
    py = 0
    plt.scatter(px, py, color="blue")
    plt.scatter(get_x(px, py, alpha), get_y(px, py, alpha), color="red")

plt.show()